name: C/C++ CI

on: [push]

jobs:
  ubuntu-build:
    - uses: actions/checkout@master
    runs-on: ubuntu-latest
    steps:
    - name: Build project
      run: make -j 4
    - name: Run project
      run: ./build/hello
